<h2><%= @listing.title %></h2>

<div style="margin-top: 2rem">
  <%= link_to "Add Device", edit_listing_path(@listing), class: "btn btn-sm btn-info" %>
  <%= link_to "Edit Device", edit_listing_path(@listing), class: "btn btn-sm btn-secondary" %>
  <%= link_to "Delete Device", edit_listing_path(@listing), class: "btn btn-sm btn-danger", method: :delete, data: {confirm: "Are you sure you want to delete this device?"} %>

</div>
<hr>
<div style="margin-top: 3rem">
  <%= link_to "Back", root_path, class: "btn btn-sm btn-primary"%>
  <%= link_to "Edit Listing", edit_listing_path(@listing), class: "btn btn-sm btn-secondary" %>
  <%= link_to "Delete Listing", listing_path(@listing), class: "btn btn-sm btn-danger", method: :delete, data: {confirm: "Are you sure you want to delete this listing?"} %>
</div>
